# Cloudflare bindings — Agent Ledger (dev)

Worker: `agent-ledger-runtime-dev`

| Binding | Type | Status |
|---------|------|--------|
| KV runtime config | KV | Optional |
| Queue agent events | Queue | Optional |
| R2 exports | R2 | Optional |

Primary DB: Supabase `caesar-agent-ledger-dev` schema `agent_ledger`. Documented only — not provisioned in T012.
