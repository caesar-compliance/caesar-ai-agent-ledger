# Agent Ledger runtime Worker (dev)

Routes: `/healthz`, `/readyz`, `/version`, `POST /events` (disabled by default).

Deploy: `wrangler deploy --name agent-ledger-runtime-dev` or GitHub `dev-runtime-activate.yml`.
